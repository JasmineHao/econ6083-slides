# 🚀 一键部署指南

## 快速开始

你有两种方式部署课件网站：

### 方式 1：运行自动脚本（推荐）

在 presentations 目录下运行：

**Windows:**
```bash
deploy.bat
```

**Linux/Mac:**
```bash
./deploy.sh
```

脚本会自动完成：
1. ✓ Git 仓库已初始化
2. ✓ 添加所有文件并提交
3. ✓ 配置远程仓库
4. ✓ 推送到 GitHub
5. ✓ 提供 GitHub Pages 配置说明

### 方式 2：手动部署

如果自动脚本遇到问题，可以手动执行以下步骤：

#### 步骤 1: 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 仓库名称: `econ6083-slides`
3. 设置为 **Public**（公开）
4. **不要**勾选 "Add a README file"
5. 点击 "Create repository"

#### 步骤 2: 推送代码

在 presentations 目录下运行：

```bash
# 添加远程仓库（替换 YOUR-USERNAME 为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR-USERNAME/econ6083-slides.git

# 提交文件
git add .
git commit -m "Initial commit: ECON6083 presentations setup"

# 设置主分支并推送
git branch -M main
git push -u origin main
```

#### 步骤 3: 配置 GitHub Pages

1. 打开仓库页面: `https://github.com/YOUR-USERNAME/econ6083-slides`
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**
4. 在 "Source" 下拉菜单中选择 **"GitHub Actions"**
5. 保存设置

#### 步骤 4: 等待部署完成

- GitHub Actions 会自动构建和部署（约 2-3 分钟）
- 在仓库的 **Actions** 标签页可以查看进度
- 完成后访问: `https://YOUR-USERNAME.github.io/econ6083-slides/`

## 🎓 后续更新课件

每次更新课件后：

```bash
# 1. 编辑 _slides/ 中的 .md 文件

# 2. 本地预览（可选）
npm start

# 3. 构建（可选，GitHub Actions 会自动构建）
npm run build
npm run build:index

# 4. 提交并推送
git add .
git commit -m "Update: 添加第X讲课件"
git push
```

推送后，GitHub Actions 会自动重新部署网站！

## 📋 已完成的准备工作

✅ Git 仓库已初始化
✅ npm 依赖已安装
✅ GitHub Actions 配置已就绪（.github/workflows/deploy.yml）
✅ 部署脚本已创建（deploy.bat / deploy.sh）
✅ 第8讲课件已准备

## 🔧 本地开发

```bash
# 安装依赖（如果还没安装）
npm install

# 启动开发服务器（支持热重载）
npm start
# 访问 http://localhost:8080

# 构建所有幻灯片
npm run build

# 生成首页索引
npm run build:index

# 导出为 PDF（可选）
npm run pdf
```

## 💡 提示

- **自动部署**: 每次推送到 main 分支，GitHub Actions 会自动部署
- **自定义域名**: 在仓库 Settings → Pages 中可以配置自定义域名
- **私有仓库**: 如果设置为 Private，需要 GitHub Pro 账号才能使用 Pages
- **构建日志**: 在仓库的 Actions 标签页查看部署日志和错误信息

## ❓ 常见问题

### 推送失败：需要身份验证

**解决方案**：生成 Personal Access Token

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 复制生成的 token
5. 推送时使用 token 作为密码

### GitHub Pages 没有自动部署

**检查清单**：
- ✓ 仓库设置中 Pages Source 选择了 "GitHub Actions"
- ✓ .github/workflows/deploy.yml 文件存在
- ✓ 代码已成功推送到 main 分支
- ✓ 在 Actions 标签页查看是否有错误

### 网站显示 404

- 等待 2-3 分钟让部署完成
- 确认 GitHub Pages 已启用
- 检查 URL 格式: `https://用户名.github.io/仓库名/`

---

**准备好了吗？运行 `deploy.bat` 开始部署！** 🎉
